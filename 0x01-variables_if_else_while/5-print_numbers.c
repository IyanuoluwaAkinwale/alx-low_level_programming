#include <stdio.h>
#include <math.h>

/**
 * main - entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int b = 0;

	while (b < 10)
	{
		printf("%d", b);
		b++;
	}

	printf("\n");

	return (0);
}
